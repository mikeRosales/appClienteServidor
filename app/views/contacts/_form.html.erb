<%= form_for(@contact) do |f| %>
  <% if @contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contact.errors.count, "error") %> prohibited this contact from being saved:</h2>

      <ul>
      <% @contact.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
  <div class="small-8 medium-centered columns">
    <div class="field">
      <%= f.label :Nombre_del_Contacto %><br>
      <%= f.text_field :contact_name %>
    </div>
     <%= f.label :Seleccione_a_cual_proyecto_agregarlo %><br>
   <%=  f.collection_select(:project_id, current_user.projects, :id, :project_name) %>
    <div class="actions">
      <button name="commit" type="submit" value="Ingresar">Agregar</button>
    </div>
  </div>
</div>
<% end %>
